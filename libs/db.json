{
    "host":"localhost",
    "username":"root",
    "password":"",
    "database":"db_test_2",
    "port":3306,
    "showErrors":true,

    "structure":{
        "user": [
            "id UUID DEFAULT UUID() PRIMARY KEY",
            "created_at BIGINT UNSIGNED NOT NULL DEFAULT FLOOR(UNIX_TIMESTAMP(CURRENT_TIMESTAMP(3))*1000)",
            "private CHAR(16) NOT NULL UNIQUE",
            "username VARCHAR(64) NOT NULL UNIQUE",
            "first_name VARCHAR(64)",
            "last_name VARCHAR(64)",
            "access TINYINT NOT NULL DEFAULT 0",
            "created_by UUID",
            "dni VARCHAR(32)",
            "birth_date DATE",
            "color CHAR(6)",
            "FOREIGN KEY (created_by) REFERENCES user(id)"
        ],
        "session_token": [
            "id UUID DEFAULT UUID() PRIMARY KEY",
            "created_at BIGINT UNSIGNED NOT NULL DEFAULT FLOOR(UNIX_TIMESTAMP(CURRENT_TIMESTAMP(3))*1000)",
            "user_id UUID",
            "FOREIGN KEY (user_id) REFERENCES user(id)"
        ],
        "event_in": [
            "id UUID DEFAULT UUID() PRIMARY KEY",
            "created_at BIGINT UNSIGNED NOT NULL DEFAULT FLOOR(UNIX_TIMESTAMP(CURRENT_TIMESTAMP(3))*1000)",
            "user_id UUID",
            "FOREIGN KEY (user_id) REFERENCES user(id)"
        ],
        "event_out": [
            "id UUID DEFAULT UUID() PRIMARY KEY",
            "created_at BIGINT UNSIGNED NOT NULL DEFAULT FLOOR(UNIX_TIMESTAMP(CURRENT_TIMESTAMP(3))*1000)",
            "event_in_id UUID NOT NULL UNIQUE",
            "user_id UUID",
            "FOREIGN KEY (user_id) REFERENCES user(id)"
        ],
        "image":[
            "id UUID DEFAULT UUID() PRIMARY KEY",
            "created_at BIGINT UNSIGNED NOT NULL DEFAULT FLOOR(UNIX_TIMESTAMP(CURRENT_TIMESTAMP(3))*1000)",
            "name VARCHAR(64)",
            "description VARCHAR(256)",
            "format VARCHAR(64) NOT NULL",
            "height BIGINT NOT NULL",
            "width BIGINT NOT NULL",
            "created_by UUID",
            "FOREIGN KEY (created_by) REFERENCES user(id)"
        ],
        "department":[
            "id UUID DEFAULT UUID() PRIMARY KEY",
            "created_at BIGINT UNSIGNED NOT NULL DEFAULT FLOOR(UNIX_TIMESTAMP(CURRENT_TIMESTAMP(3))*1000)",
            "name VARCHAR(64)",
            "description VARCHAR(256)"
        ],
        "access":[
            "id UUID DEFAULT UUID() PRIMARY KEY",
            "created_at BIGINT UNSIGNED NOT NULL DEFAULT FLOOR(UNIX_TIMESTAMP(CURRENT_TIMESTAMP(3))*1000)",
            "name VARCHAR(64)",
            "description VARCHAR(256)"
        ],
        "email":[
            "id UUID DEFAULT UUID() PRIMARY KEY",
            "created_at BIGINT UNSIGNED NOT NULL DEFAULT FLOOR(UNIX_TIMESTAMP(CURRENT_TIMESTAMP(3))*1000)",
            "email VARCHAR(64)",
            "added_by UUID",
            "FOREIGN KEY (added_by) REFERENCES user(id)"
        ]
    },
    
    "finally": [
       
    ]
}